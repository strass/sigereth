import { Interpolation } from '@emotion/core';

export const allSmallCaps: Interpolation = {
  fontVariantCaps: 'all-small-caps',
};

export const textAlignCenter: Interpolation = {
  textAlign: 'center',
};
